# Demo laboratory

A lightweight Vite 8 + React 19 application containing six focused projects:

1. measured fragments;
2. editorial contour;
3. documentation corners;
4. conversation surface;
5. mixed-direction content;
6. dense records.

The URL hash selects a project without adding a routing dependency.

```bash
pnpm --filter @round-selection/demo dev
```
