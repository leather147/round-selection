# Web landing

Next.js 16 App Router landing page for Round Selection.

```bash
pnpm --filter @round-selection/web dev
```

The page is server-rendered by Next.js and hydrates one interactive client island. Motion uses `LazyMotion` with `domAnimation`; scrolling remains native. Set `NEXT_PUBLIC_DEMO_URL` when the demo laboratory is deployed separately.
